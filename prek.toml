[[repos]]
repo = "builtin"
hooks = [
    { id = "check-yaml" },
    { id = "check-toml" },
    { id = "end-of-file-fixer" },
]

[[repos]]
repo = "local"
hooks = [
    { id = "cargo-clippy", name = "cargo clippy", entry = "cargo clippy --target wasm32-wasip2 -- -D warnings", language = "system", pass_filenames = false, always_run = true },
    { id = "cargo-fmt", name = "cargo fmt", entry = "cargo fmt -- --check", language = "system", pass_filenames = false, always_run = true },
]
